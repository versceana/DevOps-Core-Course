apiVersion: v1
kind: Service
metadata:
  name: python-app-bg-active
spec:
  selector:
    app: python-app-bg
  ports:
    - port: 80
      targetPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: python-app-bg-preview
spec:
  selector:
    app: python-app-bg
  ports:
    - port: 80
      targetPort: 80
